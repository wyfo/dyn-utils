[workspace]
members = ["macros"]

[package]
name = "dyn-utils"
version = "0.1.0"
edition = "2024"

[features]
default = ["alloc"]
alloc = []

[dependencies]
elain = "0.3.0"
dyn-utils-macros = { path = "macros" }

[dev-dependencies]
async-trait = "0.1.89"
divan = "0.1"
dynify = "0.1.2"

[[bench]]
name = "comparison"
harness = false

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ['cfg(coverage_nightly)'] }
